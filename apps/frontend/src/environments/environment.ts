/** Interfaces */
import { IEnvironment } from './environment.interface';

/** Configuración de entorno de desarrollo; el backend escucha en el puerto 3000 */
export const environment: IEnvironment = {
  apiBaseUrl: 'http://localhost:3000',
};
